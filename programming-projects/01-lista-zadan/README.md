# Lista zadań (HTML + CSS + JavaScript)

Aplikacja to-do list w czystym JavaScripcie, bez frameworków.

Uruchomienie: otwórz `index.html` w przeglądarce.

Pliki: `index.html`, `style.css`, `script.js`
