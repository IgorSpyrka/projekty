# Analizator wydatków (Python)

Skrypt wczytujący wydatki z CSV, liczący podsumowanie wg kategorii (`pandas`)
i rysujący wykres (`matplotlib`).

Uruchomienie:

```bash
pip install pandas matplotlib
python3 analizator.py
```

Pliki: `analizator.py`, `wydatki.csv`, `wykres_wydatkow.png`
